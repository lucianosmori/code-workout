from setuptools import setup, find_packages

setup(
    name='',
    version='0.0.1',
    description='Python challenges',
    author='Luciano',
    packages=find_packages(),
    install_requires=[
        'pytest'
    ],
)